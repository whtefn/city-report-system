PORT=5000

# JWT
JWT_SECRET=change-me-to-a-long-random-secret

# SQLite
DATABASE_PATH=./data/city_reports.db

# SMTP (Nodemailer)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM=noreply@example.com
